import {Router} from "express"

const clientsRouter = Router();

// clientsRouter.post("/customers",)
// clientsRouter.get("/customers",)
// clientsRouter.put("/customers",)

export default clientsRouter;